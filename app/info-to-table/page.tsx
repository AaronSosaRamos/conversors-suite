"use client"

import InfoToTableForm from '@/components/InfoToTableForm';
import React from 'react';

const JsonToSQLPage: React.FC = () => {
  return (
      <InfoToTableForm darkMode={false} />
  );
};

export default JsonToSQLPage;
