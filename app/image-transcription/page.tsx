"use client"

import ImageTranscriptionForm from '@/components/ImageTranscriptionForm';
import React from 'react';

const JsonToSQLPage: React.FC = () => {
  return (
      <ImageTranscriptionForm darkMode={false} />
  );
};

export default JsonToSQLPage;
